const studentInfo = {
  name: 'Smith',
  age: 20,
  gender: 'Male',
};

const { name, age, gender } = studentInfo;

console.log(studentInfo);
console.log(name);
console.log(age);
console.log(gender);

const studentArray = [
  ['Jennifer', 22, 'Male'],
  ['Bobby', 25, 'Female'],
];

console.log(studentArray[0][1]);
